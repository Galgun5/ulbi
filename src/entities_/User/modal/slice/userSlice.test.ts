import { userReducer, userActions } from './userSlice';

describe('counterSlice', () => {
    test('should work with empty state', () => {
        // expect(userReducer(undefined, userActions)).toEqual({ value: 1 });
    });
});
